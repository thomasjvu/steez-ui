# Implementation Plans

Generated by the improve skill on 2026-07-29. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

**Repo note:** This tree had **no `.git` directory** when plans were written.
`Planned at` uses `no-git`. Before `execute`, initialize git (or open a clone
that is a real repo) so worktree isolation works.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish verification baseline (typecheck + tests) | P1 | M | — | DONE (merged) |
| 002 | Rewrite steez registry generator + sync payloads | P1 | M | 001 recommended | TODO |
| 003 | Fix registry install URL consistency | P1 | S | — | DONE (merged) |
| 004 | Make `three` optional for `@steez-ui/ui` | P2 | M | 001 recommended | TODO |
| 005 | Code-split docs component previews | P2 | M | — | TODO |
| 006 | Fix publish script (LICENSE + pnpm) | P1 | S | — | DONE (merged) |
| 007 | Clear LoadingScreen chained timeouts | P2 | S | — | DONE (merged) |
| 008 | Pause WebGL when hidden / reduced-motion | P2 | S–M | — | DONE (merged) |
| 009 | Document dual package surfaces + AGENTS.md | P2 | S | — | DONE (merged; commit fixed after executor left uncommitted) |
| 010 | Real authoring model on `/docs` | P3 | M | 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- **001 first** when possible: gives typecheck/test gates for later plans.
- **002** rewrites registry generation and regenerates `public/r-steez` (includes missing `fui-button-tile` and Button source drift).
- **003** can run in parallel with 001/002 (docs URL fixes only); re-check after 002 if generator rewrites index links.
- **004** changes package public surface; run tests from 001 if present.
- **006** is independent release tooling.
- **010** should land after 002 so authoring docs describe real commands.

## Findings considered and rejected

- Bare `registryDependencies` absolute URLs: needs live `shadcn@3` confirmation before bulk rewrite (plan 002 may emit names as today).
- Full merge of `@steez-ui/ui` and `@steez-ui/react` APIs: HIGH risk without consumer inventory; plan 009 is docs/contract only.
- Empty `registry/new-york` deletion: defer until product decision on Boston demos.
- npm OTP on argv: low product impact; prefer env-only when touching publish script in 006 if trivial.
